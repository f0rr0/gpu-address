{
  "name": "gpu-postal-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core"
  ],
  "scripts": {
    "build": "npm run build --workspace packages/core",
    "test": "npm run build && npm test --workspace packages/core",
    "test:parity": "node packages/core/test/parity.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
